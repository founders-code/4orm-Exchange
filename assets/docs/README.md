# Documents folder

Drop the actual PDFs/XLSXs into this folder using the exact filenames listed in the project README. Each filename is already wired into `data-room.html`, so a file showing up here makes its link work immediately.

If you want to change a filename, update both this filename and the matching `href` in `data-room.html`.
